# diss-project

Please run the docker-compose command to start the project.

```bash

docker compose up
```

If you want to run the project in the background, you can use the `-d` flag:

```bash

docker compose up -d
```

If you want to do development, you can simply just run the postgres container.